/**
 * Site-wide configuration the user edits after signing up for third-party
 * services. Each constant documents exactly what to paste.
 */

/**
 * Calendly booking link for the "Book a 20-min intro" button in ContactSection.
 *
 * TODO(user): Sign up at https://calendly.com, create a 20-minute event type,
 * and paste the public URL here (e.g. "https://calendly.com/scisymbio/intro").
 * Leave as an empty string to hide the button.
 */
export const CALENDLY_URL = "";
